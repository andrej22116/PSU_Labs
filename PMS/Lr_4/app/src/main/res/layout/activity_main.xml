<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:layout_weight="1" >

        <TextView
            android:id="@+id/tvSelectedItem"

            android:layout_width="fill_parent"
            android:layout_height="40dp" />

        <Button
            android:id="@+id/btnSelectItem"
            android:text="Выбрать станцию"

            android:layout_width="fill_parent"
            android:layout_height="wrap_content"

            android:onClick="onSelectMetroStation" />

        <Button
            android:id="@+id/btnSelectItemOther"
            android:text="Выбрать станцию (неявное намерение)"

            android:layout_width="fill_parent"
            android:layout_height="wrap_content"

            android:onClick="onSelectMetroStationOther" />
    </LinearLayout>


</android.support.constraint.ConstraintLayout>