<?php	
	if ( !empty($_GET['pulse-width']) ) {
		//exec("./powerControl " + "pulse-width" + $_GET['pulse-width']);
	}
	else if ( !empty($_GET['power']) ) {
		//exec("./powerControl " + "power" + $_GET['pulse-width']);
	}
	else if ( !empty($_GET['blick']) ) {
		//exec("./blick");
	}
?>